# -------------------------------
# 🔧 ENTORNO DE EJECUCIÓN
# -------------------------------
NODE_ENV=     # Opciones: development | production | test

# -------------------------------
# 🌐 CONFIGURACIÓN GENERAL
# -------------------------------
PORT=3000                # Puerto donde se ejecuta la API

# -------------------------------
# 🌎 FECHA Y TIEMPO
# -------------------------------
TIME_ZONE=America/Guatemala     # Zona horaria usada por la API (ej. UTC, America/Argentina/Buenos_Aires)

# (opcional) Fecha simulada para pruebas
MOCK_DATE=#2024-12-25T10:00:00Z    # Si se desea fijar una fecha específica para pruebas (ISO 8601)

# -------------------------------
# 🗄️ CONEXIÓN A BASE DE DATOS
# -------------------------------
DB_TYPE=mysql             # Tipo de base de datos (mysql, postgres, sqlite, etc.)
DB_HOST=localhost
DB_PORT=3306
DB_USERNAME=
DB_PASSWORD=
DB_SYNCHRONIZE=       # ⚠️ true solo en desarrollo, false en producción
DB_LOGGING=false
DB_DATABASE=

# -------------------------------
# 🔐 AUTENTICACIÓN / JWT
# -------------------------------
JWT_SECRET=una_clave_secreta_segura
JWT_EXPIRES_IN=1d          # Tiempo de expiración del token (ej: 15m, 1h, 7d)

# -------------------------------
# 📦 OTROS SERVICIOS EXTERNOS
# -------------------------------
# API_CLIENT_URL=http://localhost:4200   # URL del cliente frontend
SOME_API_KEY=xxxx-xxxx-xxxx            # Clave para API externa (ej. Stripe, Firebase)  
